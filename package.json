{
  "name": "chqin",
  "private": "true",
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @chqin/web",
    "dev:api": "npm run dev --workspace @chqin/api",
    "build": "npm run build --workspace @chqin/web",
    "preview": "npm run preview --workspace @chqin/web",
    "lint": "oxlint",
    "db:up": "docker compose up -d",
    "db:migrate": "npm run migrate --workspace @chqin/api",
    "db:psql": "docker exec -it chqin-postgres psql -U chqin -d chqin"
  },
  "devDependencies": {
    "oxlint": "^1.75.0"
  }
}
